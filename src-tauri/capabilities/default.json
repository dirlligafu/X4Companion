{
  "identifier": "default",
  "description": "Default capability — extended as features are added",
  "windows": ["main", "dictionaries"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "store:default",
    "opener:allow-reveal-item-in-dir"
  ]
}
